extends layout

block content

  h1 Brands: #{brand.name}

  div(style='margin-left:20px;margin-top:20px')

    h4 Brand added to cart

    dl
      each brand in brand_products
        dt
          a(href=book.url) #{book.title}
        dd #{book.summary}

      else
        p This genre has no books