extends layout

block content 
    h1= title 
    p
      a(href='/inventory/categories/add') Add new category

    ul 
        each category in category_list 
            li 
                a(href=category.url) #{category.name}

        else 
            li There is no category yet, add it by 
            a(href="/category/add") clicking here