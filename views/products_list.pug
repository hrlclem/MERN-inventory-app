extends layout

block content 
    h1= title 

    ul 
        each product in list_product 
            li 
                a(href=product.url) #{product.name}

        else 
            li There is no brand yet, add it by 
            a(href="/product/add") clicking here